"""Base class for SQLAlchemy models."""

# Import the BaseModel from the models directory
from app.models.base_model import BaseModel

# Use the BaseModel as our base class for all models
Base = BaseModel 